apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-recurring-task-service
  labels:
    app: recurring-task-service
    app.kubernetes.io/name: recurring-task-service
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: recurring-task-service
    app.kubernetes.io/instance: {{ .Release.Name }}
