{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: activity-logger
  labels:
    app: activity-logger
    app.kubernetes.io/name: activity-logger
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: activity-logger
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
